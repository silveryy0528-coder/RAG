# Minimal runtime requirements for RAG (keep this list small and explicit)
# Note: sentence-transformers depends on PyTorch — install a compatible torch
# build for your platform before (see README.md for guidance).

numpy
sentence-transformers
faiss-cpu
openai
transformers
huggingface-hub
pymupdf
pyyaml
